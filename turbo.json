{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "inputs": ["src/**", "tsconfig.json", "../../tsconfig.base.json"]
    },
    "dev": {
      "cache": false
    }
  }
}
